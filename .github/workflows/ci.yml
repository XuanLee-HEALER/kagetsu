name: CI

# 聚焦 kagetsu-core: fmt + clippy + 单元测试 + 覆盖率。
# 测试只跑 --lib (403 单元测试), 对齐 justfile 的 `just ci` 惯例 ——
# tests/ 下的 4-swarm libp2p 集成与 proptest fuzz 偏重 / 偶发 flaky,
# 不进 CI, 由 `just fuzz` 等本地配方按需跑。

on: [push, pull_request]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: fmt · clippy · test (kagetsu-core)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt -p kagetsu-core -- --check

      - name: Clippy
        run: cargo clippy -p kagetsu-core --all-targets --features dev-tools -- -D warnings

      - name: Test (lib)
        run: cargo test -p kagetsu-core --lib --features dev-tools

  coverage:
    name: coverage (kagetsu-core)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Generate coverage (lcov)
        run: cargo llvm-cov -p kagetsu-core --lib --features dev-tools --ignore-run-fail --lcov --output-path lcov.info

      - name: Upload to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: lcov.info
          flags: kagetsu-core
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
